.row
	.col-md-9
		- @shows.each_with_index do |show, index|
			.col-md-3
				.center-block.text-center{ :style => 'padding-bottom: 30px'}
					=link_to image_tag("missing_show.png", :width => 110, :height => 110), show_url(show.id)
					%br
					%small=link_to show.name, show_url(show.id)
	.col-md-3
		%h4.text-center Recent Episodes
